
<h1>Burgers!</h1>

<img class="img-fluid image" src="https://image.freepik.com/free-vector/yellow-background-with-appetizing-burger-in-flat-design_23-2147634054.jpg">

<form class="create-form">
  <h2>Add a Burger</h2>
  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>

<div class="row">
  <div class="col-md-6">

    <h2>Burgers ready to be ate: </h2>

    <ul>
      {{#each burgers}}
        {{#unless ate}}
          {{> burgers/burger-block ate=true}}
        {{/unless}}
      {{/each}}
    </ul>
  </div>
  <div class="col-md-6">
    <h2>Burgers that been ate!</h2>

    <ul>
      {{#each burgers}}
        {{#if ate}}
          {{> burgers/burger-block ate=false}}
        {{/if}}
      {{/each}}
    </ul>
  </div>

  

